interface:
  display_name: "Storybook"
  short_description: "Run one external Storybook for declared package owners"
  default_prompt: "Use $storybook to attach declarations and verify an exact package tab on the one external server."

policy:
  allow_implicit_invocation: true
