interface:
  display_name: "Storybook"
  short_description: "Operate the one external Storybook through MCP"
  default_prompt: "Use only Storybook MCP tools to ensure, search, open, wait, inspect, interact, and capture an exact package view."

policy:
  allow_implicit_invocation: true
