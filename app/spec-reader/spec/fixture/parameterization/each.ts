import {describe, test} from "bun:test"

/**
Образец сочетания условного запуска и параметризации.

@remarks
Условие false сохраняет группу активной. skipIf присутствует для проверки
распознавания цепочки модификаторов анализатором.
*/
describe.skipIf(false).each([{name: "Группа"}])("$name", () => {
  test.each([{name: "Проверка"}])("$name", () => {})
})
